/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        nextDevTools: false,
        devIndicators: false
    },
    devIndicators: false
};

module.exports = nextConfig;
